# Vue Demo

这是一个使用 Vite + Vue 3 搭建的演示项目。

快速开始：

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 打包
npm run build

# 预览生产构建
npm run preview
```

建议使用 Node.js 16+ 或更高版本。若想使用 yarn：

```bash
yarn
yarn dev
```
